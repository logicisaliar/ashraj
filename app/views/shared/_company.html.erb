<div class="vertical">
  <div class="col-xs-10 col-xs-offset-1 col-md-4 col-md-offset-4 form-default">
    <%= simple_form_for order, url: client_orders_path do |f| %>
    <%= f.error_notification %>
    <%= f.input :company_id, collection: @companies %>
    <%#= f.input :company_id,
 as: :grouped_select, collection: @companies,  group_method: :last %>
    <%= f.button :submit, "Submit", class: "btn-submit" %>
    <% end %>
  </div>

  <%= link_to '<i class="fas fa-plus"></i> Add New Company'.html_safe, new_company_path, class: "btn btn-default btn-top-m col-xs-10 col-md-4 col-xs-offset-1 col-md-offset-4" %>
</div>
