<div class="order-card">
<h4>
  <strong><%= o.order_num %> - <%= o.status.capitalize %></strong>
</h4>
<h6>
  <strong><%= link_to "#{o.company.name}", client_order_path(o) %></strong>
</h6>
<h6>
  <%= o.address.pincode.city.name %>, <%= o.address.pincode.city.state.name %>
</h6>
</div>
