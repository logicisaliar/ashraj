<% if @order.company.nil? %>
  <%= render "/shared/company", order: @order %>
<% elsif @order.company.addresses.length > 1 %>
  <%= render "/shared/address", order: @order %>
<% elsif @order.lines.length == 0 %>
  <% redirect_to new_line_path %>
<% else %>
  YOOOOOOOO
<% end %>
